package com.ib.datastructures.linkedlist;

public class Stack<T> extends LIFO<T>{
    // TODO: Implement LIFO methods
    // A Stack is a linked list data structure that follows the LIFO (Last In First Out) principle.
    // This means that the last element added to the stack will be the first one to be removed.
    // Method push() adds an element to the stack.
    // Method pop() removes and returns the last element of the stack.
    // Method peek() returns the last element of the stack without removing it.
    // Method size() returns the number of elements in the stack.
    // What is the time complexity of each method?

    @Override
    public void push(T value) {

    }

    @Override
    public T pop() {
        return null;
    }

    @Override
    public T peek() {
        return null;
    }

    @Override
    public int size() {
        return 0;
    }
}
